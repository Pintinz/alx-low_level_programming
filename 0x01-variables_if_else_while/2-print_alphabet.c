#include <stdio.h>
#include <stdlib.h>
/**
 * main - This contain all codes in the program
 * Return: 0 (successfull)
 */
int main(void)
{
	char m = 'a';

	while (m <= 'z')
	{
	putchar(m);
	m = m + 1;
	}
return (0);
}
