#include <stdio.h>
#include "main.h"

/**
 * mul - a function that multiplies two integer
 * @a: one of the integer
 * @b: the other of the integer
 * Return: Always 0
 */

int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
