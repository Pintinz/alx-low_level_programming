#include "main.h"

/**
 * _isdigit - a function that checks for a
 * digit (0 through 9).
 * @c: the digit the function checks
 * Return: either 1 or 0
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')
	{
		return (1);
	}
	else
	{
		return (0);
	}

return (0);
}
