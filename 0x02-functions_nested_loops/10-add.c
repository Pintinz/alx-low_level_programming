#include "main.h"
/**
 * add - function that calls int i and l.
 * @i: the second interger
 * @l: the second interger.
 * Return: always 0
 */
int add(int i, int l)
{
	int n;

	n = i + l;
	return (n);
}
