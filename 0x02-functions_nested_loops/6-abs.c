#include "main.h"
#include <stdio.h>
/**
 * _abs - function that calls absolute value.
 * @int: the number to absolute
 * Return: Always 0.
 */
int _abs(int c)
{
	if (c < 0)
	{
		n = -n;
	}
	return (0);
}
