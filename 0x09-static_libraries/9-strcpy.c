#include "main.h"

/**
 * _strcpy - a function that returns a value
 * @dest: the value printed
 * @src: the character printed
 * Return: always 0
 */

char *_strcpy(char *dest, char *src)
{
	return (dest);
}
