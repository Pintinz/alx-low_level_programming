#include "main.h"

/**
 * _strspn - a function that returns a value
 * @s: the value printed
 * @accept: the character printed
 * Return: always 0
 */

unsigned int _strspn(char *s, char *accept)
{
	int n;

	n = *s;

	return (n);
}
