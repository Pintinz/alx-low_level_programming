#include "main.h"

/**
 * _memset - a function that returns a value
 * @s: the value printed
 * @b: value to be printed
 * @n: value to be printed
 * Return: always 0
 */

char *_memset(char *s, char b, unsigned int n)
{
	return (s);
}
