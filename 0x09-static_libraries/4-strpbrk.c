#include "main.h"

/**
 * _strpbrk - a function that returns a value
 * @s: the value printed
 * @accept: the character printed
 * Return: always 0
 */

char *_strpbrk(char *s, char *accept)
{
	return (s);
}
