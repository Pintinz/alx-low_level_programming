#include "main.h"

/**
 * _memcpy - a function that returns a value
 * @dest: the value printed
 * @src: the value printed
 * @n: the value to be printed
 * Return: always 0
 */

char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (dest);
}
