#include "main.h"

/**
 * _strncpy - a function that returns a value
 * @dest: the value printed
 * @src: the character printed
 * @n: the number of times 
 * Return: always 0
 */

char *_strncpy(char *dest, char *src, int n)
{
	return (dest);
}
